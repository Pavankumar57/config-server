server:
  port: 8010

message: dev
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8030/eureka/
  instance:
    hostname: localhost